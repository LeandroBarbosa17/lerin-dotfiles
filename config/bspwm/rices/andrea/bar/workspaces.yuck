;; Workspaces ;;
(deflisten workspace0 "scripts/WorkSpaces HDMI-A-1")
;; Workspaces Widgets ;;
(defwidget workspace0 [] (literal :content workspace0))
;; Workspaces Main Widget ;;
(defwidget workspaces [monitor]
 (box :halign "center"
 :spacing 15
 :space-evenly "false"
 (box :visible { monitor=="HDMI-A-1" } (workspace0))
))